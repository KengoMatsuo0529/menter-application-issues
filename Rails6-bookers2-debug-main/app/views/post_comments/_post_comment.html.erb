 <div class='row'>
    <div class="col-md-8">
      <% book_detail.post_comments.each do |post_comment| %>
      <%= post_comment.comment %>
        <% if post_comment.user == current_user %>
          <%= button_to "削除", book_post_comment_path(post_comment.book, post_comment), method: :delete, remote: true %>
        <% end %>
      <% end %>
    </div>  
  </div>